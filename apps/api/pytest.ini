[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
addopts = -ra --strict-markers
markers =
    unit: fast, no external dependencies
    integration: exercises the FastAPI app through its HTTP interface (may need a live DB for DB-touching endpoints)

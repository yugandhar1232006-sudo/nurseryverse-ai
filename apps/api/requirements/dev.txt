-r base.txt
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==5.0.0
httpx==0.27.2
aiosqlite==0.22.1
ruff==0.6.9
mypy==1.11.2
black==24.8.0
faker==30.1.0
import-linter==2.1
# Phase 6 Module 14 (Production Readiness) -- docs/architecture/10-devops.md
# §1's CI pipeline "Security scan - dependency vulnerabilities, SAST" stage
# (Task #162's .github/workflows/ci.yml). pip-audit checks requirements/
# base.txt's pinned versions against the PyPI Advisory Database; bandit
# statically scans app/ source for common Python security anti-patterns
# (hardcoded secrets, unsafe deserialization, etc.) -- two different
# concerns ("known-vulnerable dependency" vs. "code pattern that's risky
# regardless of dependency versions"), both named explicitly in the doc.
pip-audit==2.7.3
bandit==1.7.10
# Phase 6 Module 14 (Production Readiness) defect fix: `mypy app` (part of
# this module's own full validation pass) reported "Library stubs not
# installed" for openpyxl/reportlab/python-jose/celery -- a pre-existing
# gap (these libraries have been dependencies since Modules 2/11/12, but
# no prior module's validation pass apparently ran a full, stub-complete
# `mypy app` against them). types-cloudinary does not exist on PyPI as of
# this module -- see pyproject.toml's `[[tool.mypy.overrides]]` for that
# one remaining gap, closed via a scoped ignore_missing_imports instead.
types-openpyxl==3.1.5.20260807
types-python-jose==3.5.0.20260408
types-reportlab==4.2.5.20241221
celery-types==0.26.0
